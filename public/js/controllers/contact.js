angular.module('mean.system').controller('ContactController', ['$scope', 'Global', 'Resources', function ($scope, Global, Resources) {
    $scope.global = Global;
    
    //TODO: make the form wider
}]);